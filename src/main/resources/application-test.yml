spring:
  datasource:
    url: jdbc:h2:mem:DBTest
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update